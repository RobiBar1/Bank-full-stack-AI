#include "simple_shell.h"
#include "string.h"

#define MAX_LEN 50
int ActiveShell()
{
    char input[MAX_LEN];
    printf("Enter your input: \n");
    scanf("%s", input);

    while (input /*TODO: with strcmp check if input is exit so break the loop*/)
    {
    }

    return 0;
}