#include <assert.h> /* assert */
#include <stdio.h>  /* fopen */

#include "shuffle_and_sort_dict.h" /* our api */

int DictSort(char** words, size_t amount_words)
{
    (void)words;
    (void)amount_words;

    return 0;
}